version: '3.8'

services:
  code-interpreter:
    image: e2bdev/code-interpreter
    container_name: e2b-code-interpreter
    ports:
      - "3101:3000"  # Expose container port 3000 to host port 3101
    restart: unless-stopped

